// Foundation: ring + floor + apron
// Part of the v3 build pipeline; included from build.scad.

include <../../lib/defaults.scad>
include <config.scad>
use <../../lib/bom.scad>

module v3_fundament(palette = DEFAULT_PALETTE) {
    // populated by Task A3..A8
}
