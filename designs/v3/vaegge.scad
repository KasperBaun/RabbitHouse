// All vertical stud walls (house + yard)
// Part of the v3 build pipeline; included from build.scad.

include <../../lib/defaults.scad>
include <config.scad>
use <../../lib/bom.scad>

module v3_vaegge(palette = DEFAULT_PALETTE) {
    // populated by Task A3..A8
}
